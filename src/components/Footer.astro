---
const base = import.meta.env.BASE_URL;
---
<footer class="footer bg-primary">
  <div class="container">
    <div class="footer__top">
      <div class="footer__brand">
        <img src={`${base}assets/Logo1-Blanc.svg`} alt="Jeunesse & Co" class="footer__logo" width="160" height="36" />
        <p class="mt-24" style="color: rgba(255,255,255,0.6); max-width: 300px;">
          L'accompagnement éducatif sur mesure au cœur de Genève.
        </p>
      </div>
      
      <div class="footer__nav">
        <div class="footer__col">
          <span class="footer__label">Navigation</span>
          <a href={`${base}#about`}>Notre vision</a>
          <a href={`${base}#method`}>Méthode</a>
          <a href={`${base}#team`}>L'équipe</a>
          <a href={`${base}impulsion`}>Impulsion</a>
        </div>
        <div class="footer__col">
          <span class="footer__label">Contact</span>
          <a href="mailto:info@jeunesseandco.ch">info@jeunesseandco.ch</a>
          <a href="tel:+41227311947">+41 (0)22 731 19 47</a>
          <p class="mt-16" style="color: rgba(255,255,255,0.4); font-size: 14px;">Quai du Seujet 14<br/>1201 Genève</p>
        </div>
      </div>
    </div>
    
    <div class="footer__bottom mt-80">
      <div class="footer__copyright">
        <p style="color: white; margin-bottom: 0;">© 2026 Jeunesse & Co. Tous droits réservés.</p>
      </div>
    </div>
  </div>
</footer>

<style lang="scss">
  .footer {
    padding: 100px 0 60px;
    background-color: var(--color-primary);
    
    &__top {
      display: flex;
      justify-content: space-between;
      gap: 60px;
      flex-wrap: wrap;
    }

    &__logo {
      height: 64px; // Increased size as requested previously
      width: auto;
      max-width: 100%; // Prevent overflow
      
      @media (max-width: 768px) {
        height: 48px;
      }
    }

    &__nav {
      display: flex;
      gap: 100px;
      
      @media (max-width: 768px) {
        gap: 40px;
        width: 100%;
      }
    }

    &__col {
      display: flex;
      flex-direction: column;
      gap: 16px;

      a {
        color: rgba(255,255,255,0.8);
        text-decoration: none;
        font-size: 16px;
        transition: color 0.3s ease;
        
        &:hover {
          color: var(--color-accent);
        }
      }
    }

    &__label {
      font-size: 13px;
      text-transform: uppercase;
      letter-spacing: 0.1em;
      color: var(--color-accent);
      font-weight: 700;
      margin-bottom: 8px;
    }

    &__bottom {
      border-top: 1px solid rgba(255,255,255,0.1);
      padding-top: 40px;
      display: flex;
      justify-content: center;
      align-items: center;
      color: rgba(255,255,255,0.4);
      font-size: 14px;

      .footer__copyright {
        display: flex;
        flex-direction: column;
        align-items: center;
        text-align: center;
        gap: 4px;
        
        .developer-credit {
          font-size: 12px;
          opacity: 0.6;
          margin: 0;
          color: rgba(255,255,255,0.6);
          
          a {
             transition: opacity 0.3s;
             &:hover {
                text-decoration: underline !important;
                opacity: 0.8;
             }
          }
        }
      }
    }
  }
</style>